/**
 * One-paragraph editorial blurb shown under the masthead on the home page.
 *
 * Replace the placeholder below with copy specific to your league. The page
 * renders this verbatim, so prose lives here, not in JSX.
 *
 * Aim for three to five sentences. What the league is, how long it's run,
 * what makes this version of dynasty worth caring about. Voice, not facts.
 */
export const LEAGUE_BLURB =
  "Replace this paragraph with copy about your league. A few sentences on who plays, how long it's run, and what the site is here to settle. The home page renders this verbatim, so put real voice in here, not boilerplate. Edit `src/config/about.ts` to change it.";
