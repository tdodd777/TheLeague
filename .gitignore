# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/.next-build/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (commit .env.example only)
.env
.env.local
.env.*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/
.idea/

# OS
Thumbs.db

# claude code
.claude/
CLAUDE.md

# local-only spec docs (README.md is the source of truth; these are scratch/working drafts)
DESIGN.md
FEATURE_INVENTORY.md
PRODUCT.md
RANKINGS.md
SPEC.md

# local-only SvelteKit predecessor (kept for parity reference; not deployed — see legacy-svelte tag)
reference/

# local dev artifacts
.dev.log
.edgeprofile/

# lighthouse audit output — regenerable reports, large, not source of truth
lighthouse/

# league-specific cached data — fork-specific, regenerated by `npm run ingest`
data/league-cache/
data/values-snapshots/
data/players.json

# ingest scratch space — staging holds a run's fetch before it is validated and
# promoted; .backups holds the previous live copy during the swap. Neither is
# ever the source of truth, and a CI run must not commit either.
/data/.staging/
/data/.backups/

# owner-only handoff notes (never publish)
OWNER_NOTES.md
